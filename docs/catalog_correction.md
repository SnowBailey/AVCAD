# 品牌主库（eko_catalog.json）矫正记录

> 背景：系统图只能由「真实参与音频信号链路的设备」构成。主库里混入的
> 线缆、安装架、控制面板、视频设备、服务器、软件等，一旦出现在清单里
> 就会被当成设备画进图，产生孤立节点与错误连线。
>
> 目标：让 **`no_draw`（不出图）** 成为可靠、可审计的显式标记，
> 压过「按名称关键词兜底分类」这条不可靠路径。

---

## 1. 核心机制

### 1.1 为什么不能复用 `drawable`

`catalog_resolver.resolve()` 返回的 `drawable` 是**推导值**
（`bool(spec) and 品牌未排除`），绝大多数条目都是 `False`。

拿它当「不出图」标记会误杀 **QU-16** 这类**必须靠名称兜底成 MIXER**
的型号（「数字调音台」→ MIXER）。这是设计意图，不是 bug。

→ 必须用**专用字段** `no_draw`，链路：

```
eko_catalog.json  { no_draw: true }
   ↓ catalog_resolver.Catalog.resolve()  透传
product_resolver  打 entry["_no_draw"]
   ↓ importers.build_entries()  直接排除，不走 classify_category
```

典型受益场景：「天线延长线两端BNC公头/10米」——名称含「天线」，
会被 `CATEGORY_KW` 兜底成 ANTENNA，只有 `no_draw` 能压住。

### 1.2 分类判定优先级

```
1. 主库显式 no_draw          → 排除（最高优先级）
2. 主库给了 category          → 直接用
3. 主库没给 → 名称关键词兜底   → QU-16 → MIXER 走这条路
4. 名称也命中不到 + 主库后置   → 排除，计入「已排除非信号设备」
```

### 1.3 占位项排除（importers）

造价清单里的「按需保留 / 自配 / 甲供 / 待定 / 暂定 / 按实结算 / 另计 / 不含」
这类占位行，无品牌无型号，无法建模。

`is_placeholder()` **仅在 brand 与 model 同时为空时**才排除——
否则「自配机柜」这类带型号的真实设备会被误杀。

好处：用户不必每次在第②步手动点「不需要」。

---

## 2. 矫正方法论（可复用于其他品牌）

1. **按 category 分组扫**，重点看两类：
   - `None` 类（可能漏掉该出图的设备）
   - 设备类里的**配件混入**

2. **配件词表扫描**：28 个词
   （架/罩/车/箱/线缆/线/配件/适配器/话筒夹/防风罩/机柜/电源分配…）
   + 条件「category 属设备类」→ 批量 `no_draw`

3. **⚠️ 必须留误报豁免**——纯关键词会误杀真实设备。实测 6 条：

   | 型号 | 名称 | 为什么不能排除 |
   |---|---|---|
   | AUDIX M55W / M55WHC / M55WS / M55WD | 悬吊话筒（**线缆吊挂**） | 「线缆吊挂」是安装方式描述，不是线缆配件 |
   | Apart SPH16 / SPH20 | 5"/8" 两分频垂饰球**吊挂**扬声器 | 「吊挂」是音箱形态，不是吊挂件 |

   → **批量前务必逐条看名称**，不要只看关键词命中。

4. **名称含「扬声器/音箱」的配件**（「监听扬声器配件」「音箱支架适配件」）
   会被 `CATEGORY_KW` 兜底成 SPEAKER，**只能靠主库 `no_draw` 压住**，
   `RIGGING` 关键词管不了。

5. **改前备份到 `/tmp`**，不要放仓库目录（`git add -A` 会误提交 1MB 备份；
   虽然 `.gitignore` 已含 `.bak.*`，仍优先用 `/tmp`）。

6. **改完双验证**：全量测试 + 真实清单导入/出图。

---

## 3. 已完成的轮次

| 轮次 | 提交 | 范围 | 改动 |
|---|---|---|---|
| 一 | `68e2bd2` | IPS 无线/会议主机 | 14 条非音频→不出图；停产主机→不出图；DIO 4x4→IO；**CF6300→MIC_HOST**（新建类别） |
| 二 | `ef4d1ed` | IPS 会议主机 | CF6100 停产不出图；MT110→SOURCE；MS100 不出图；CF6800WB 保持 ANTENNA |
| 三 | `53d67d6` | **IPS 全量 285 条** | 168 条（线缆 16 / 会议配件 7 / 控制面板 8 / 非音频 section 109 / 安装件 19 / 视频板卡 1 / 分类纠正 3）+ 占位项机制 |
| 四 | `d8d4f5d` | **ezacoustics 219 条** | 配件 23 + 安装件 51 + 分类纠正 2 |
| 五 | `5d970ff` | **全库 2298 条** | 配件误归设备类 84 条（EAW 42 / Community 28 / AUDIX 5 / Apart 3 / 其他 6），豁免 6 条 |

---

## 4. 全库现状

| 指标 | 值 |
|---|---|
| 总条目 | 2298 |
| `no_draw`（不出图） | 333 |
| 会出图 | 1965（其中 `None` 类别 697 条，靠名称兜底） |
| IPS 会出图 | 108 |
| ezacoustics 会出图 | 146 |

---

## 5. IPS 分类总表（已定）

```
MIC_HOST  话筒主机   ← CF6300
SOURCE    音源       ← MT110（融合会议主机电脑）、各系列会议单元、ATS 底座、模拟话筒
ANTENNA   天线       ← CF6800WB、UM2000AP / UM2000AT
ANT_DIST  分配器     ← UM2000ATD（2进/10出，cascade_outs=2）、UM2000ASD（1进/2出，暂定）
WIRELESS_RX 接收机   ← UM2002 / UM2002L / UM2002H、CF6804、CF6808
PROCESSOR 处理器     ← Aries / Libra / Gemini 全系、AM860 智能自动混音器
IO        接口       ← CF6300WB、DIO 4x4、SI Box、DI Box
no_draw   不出图     ← 停产（CF6100/CF6800系/CF6200）、服务器（MS100/MS200）、
                       软件、线缆、充电箱、中继器、控制面板、视频/中控、安装件
```

---

## 6. 待确认项

| 型号 | 现状 | 需要确认 |
|---|---|---|
| **UM2000ASD** 天线信号分线器 | 归 `ANT_DIST`，暂定 **1进/2出** | 官网（ezprointl id/2607）只写「射频信号分割/合成/偏置电压供电」，**未给端口数**。现场实际几进几出？ |
| CF68 / CF62 / CF61 系列会议单元 | 保留 `SOURCE` | 对应主机 CF6800/CF6200/CF6100 已停产不出图，单元是否也应停产？（现按「老系统维护备件仍在售」保留） |
| CF6800WB 无线数字会议天线盒 | 保持 `ANTENNA` | 与 CF6300WB（`IO`）口径不一致，是否统一？ |

---

## 7. 相关文档

- `docs/conference_cf63.md` —— IPS CF63 会讨系统连接逻辑（已落盘）
- `docs/wireless_um2000.md` —— IPS UM 系列无线真分集 + 天线级联（已实现）
- `docs/wireless_audix.md` —— AUDIX Performance 系列（已实现）
- 校正页：`/catalog`（启动服务后访问 `http://127.0.0.1:8000/catalog`）
