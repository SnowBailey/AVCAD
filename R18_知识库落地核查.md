# R18 知识库落地核查报告

> 用桌面 `测试.xlsx` 核查：R15/R16 整理的「设备类别语义知识库」(`avcad/data/device_kb.yaml` + `avcad/model/category_kb.py`) 是否真正落到了 **产品判断(第②步)** 与 **系统图生成** 逻辑里。

## 一、结论速览

| 环节 | 是否落实 | 说明 |
|------|----------|------|
| 第②步·识别建议展示 | ✅ 已落实 | `app.py` 调 `category_kb.usage_hint` 给每个模块出 `kb_hint`（这是什么 + 怎么接），前端第②步展示；`/api/legend-infer` 也用它推端口初值 |
| 类别判定·落类别（驱动出图） | ⚠️ 部分→已补(R18) | 改动前管道**从不调 `identify`**，只走主库+名称关键词兜底；R18 已把 KB 高置信识别接进兜底 |
| 系统图生成·接线逻辑 | ❌ 未落实 | `topology/`、`wires/`、`render/` 全不 import `category_kb`，出图只消费 `category` 代码字 + 端口模板 + 写死启发式，KB 的 upstream/downstream/role 仅当文档/提示 |

## 二、核查证据

### 1. 代码接线扫描（grep `category_kb`）
- 仅 `avcad/ui/app.py` 一处引用：`usage_hint`（第②步显示 + legend-infer 端口推断）。
- `importers.py` / `product_resolver.py` / `topology/` / `wires/` / `render/` **零引用**。
- ⇒ 知识库的三级识别与语义图，此前只服务于「人看的建议」，不参与「机器决策」。

### 2. 样例 `测试.xlsx` 实跑（14 台，吊架 ML210FB 被 drop → 13 台）
全部 13 台在主库 `eko_catalog` 命中，管道类别 与 `identify` 预测 **不一致数 = 0**。

```
 1 ML210      SPEAKER     ← 主库命中
 2 ML18B      SPEAKER     ← 主库命中
 3 MP120      SPEAKER     ← 主库命中
 4 MP18B      SPEAKER     ← 主库命中
 5 MP60       SPEAKER     ← 主库命中
 6 W12M       SPEAKER     ← 主库命中
 7 MP80       SPEAKER     ← 主库命中
 8 EM30D      AMP         ← 主库命中
 9 EM20D      AMP         ← 主库命中
10 EM50Q      AMP         ← 主库命中
11 GMN1208D   PROCESSOR   ← 主库命中
12 QU-16      MIXER       ← 名称兜底(调音台)/片段(qu)
13 CF2123     SOURCE      ← 主库命中
```

⚠️ **关键**：本样例「看起来全对」是假象——因为 13 台全在 `eko_catalog` 里，而 `identify` 第一级就是查同一份主库，两边必然一致。知识库的**独有价值**（KNOWN_MODELS 型号片段 + aliases 类别词）在本样例完全没被触发。

### 3. 反例（不在主库、但 KB 认得）→ 暴露 gap（R18 修复前）
`音箱管理器` 设备名含「音箱」→ 名称分类器 `CATEGORY_KW` 误判 `SPEAKER`；
KB 按型号片段 `driverack` 本应识别 `SPEAKER_MGR`（音箱管理器，非发声终点）。
修复前管道类别 = `SPEAKER`（错），KB 识别 = `SPEAKER_MGR`（对）。

## 三、R18 修复（已落地 + 加守卫）

**改动**：`avcad/workflow/importers.py`
- `apply_category_fallback` 在补类别时，先调 `category_kb.identify(brand, model, name)`；
  当 KB 返回**高置信型号片段命中（conf ≥ 0.8）** 时优先采用，覆盖名称关键词兜底；
  模糊类别词（aliases，低置信）不覆盖，保留原有广覆盖兜底，避免双源冲突。

**新增回归测试**（`avcad/tests/test_new_model_handling.py`）：
- `test_kb_model_fragment_overrides_name_classifier`：`dbx DriveRackPA2` 音箱管理器 → `SPEAKER_MGR`（KB 接管）。
- `test_kb_low_conf_keeps_name_classifier`：KB 低置信时仍走名称分类器（如 `数字调音台` → `MIXER`），不破坏原有行为。

**验证结果**：
- 新测试 2 绿；核心守卫集（9 个测试文件）**111 passed**。
- 反例重跑：`DriveRackPA2` 管道=KB=`SPEAKER_MGR`，`XLS1002`=`AMP`，`K2`=`SPEAKER`，一致。
- `测试.xlsx` 重跑：13 台仍全主库命中，管道 vs KB 不一致 = 0（修复不影响主库命中路径）。

## 四、★ 待你拍板的预存在失败（与 R18 无关，请勿混淆）

2 个测试红（已确认**非 R18 引入**，git stash 还原我的改动后仍红）：
- `test_category_kb.py::test_identify_known_models[IPS-CF6300WB-ANTENNA]`
- `test_workflow_importer.py::test_ips_discontinued_units_nodraw_and_cf6300wb_antenna`

**根因**：R17 那批**未提交**改动把 `CF6300WB` 在主库(`eko_catalog.json`)和 `MANUAL_CAT` 改成 `MIC_HOST`（无线会讨盒接主机 BOX 口），但 `device_kb.yaml` 的 `KNOWN_MODELS` 仍写 `cf6300wb→ANTENNA`，两个测试也还期望 `ANTENNA`。**主库 / KB / 测试 三处不同步**。

**当前状态**：主库 = `MIC_HOST`（R17 意图）｜ KB `KNOWN_MODELS` = `ANTENNA` ｜ 测试 = `ANTENNA`。
R18 后 `identify` 因主库第一级优先，已返回 `MIC_HOST`（与主库一致），仅测试 stale。

**需你定**：维持 R17 的 `MIC_HOST`（则把 `KNOWN_MODELS` 改 `cf6300wb→MIC_HOST` + 同步 2 个测试），还是回退成 `ANTENNA`。

> 附注：仓库现有 3 个未提交文件（`eko_catalog.json` / `legend_library.json` / `build_catalog.py`）系 R17 遗留，本次未动、未 `git checkout`（避免冲掉 R17 意图）。其中 `legend_library.json` 的大 diff 疑似之前 e2e 未保护写盘的脏数据，建议确认后再清理。

## 五、下一步建议
1. 你拍板 CF6300WB 类别（MIC_HOST / ANTENNA），我来对齐 KB + 测试。
2. 「系统图生成·接线」环节目前**仍未用** KB 的 upstream/downstream/role 语义——若希望自动布线也借鉴知识库（例如「处理器上游必接调音台、下游必接功放」），可下一轮把 `category_kb` 的语义图接入 `topology/router.py`。这是知识库目前唯一还没进逻辑的地方。
